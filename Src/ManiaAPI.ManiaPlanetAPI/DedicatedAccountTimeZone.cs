namespace ManiaAPI.ManiaPlanetAPI;

public sealed record DedicatedAccountTimeZone(string Name,
                                              bool Transitions,
                                              bool Location);